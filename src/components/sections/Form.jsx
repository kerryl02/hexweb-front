import Section from "./Section";

const Form = () => {
  return (
    <div id="Form" className="bg-sky-100">
    <Section>
      Call to action
    </Section>
    </div>

  );
};

export default Form;
